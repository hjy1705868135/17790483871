spring.profiles.active=test

# 测试使用内存 SQLite，避免污染本地文件库
app.db.type=sqlite
app.db.sqlite.driverClassName=org.sqlite.JDBC
app.db.sqlite.url=jdbc:sqlite::memory:
app.db.sqlite.username=
app.db.sqlite.password=
