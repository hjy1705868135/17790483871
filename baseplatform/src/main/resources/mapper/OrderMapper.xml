<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.md.basePlatform.repository.OrderRepository">

    <select id="selectUserOrders" resultType="com.md.basePlatform.domain.Order">
        SELECT * FROM t_order
        WHERE user_id = #{userId}
        AND deleted = 0
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
    </select>

    <select id="selectOrderItems" resultType="com.md.basePlatform.domain.OrderItem">
        SELECT 
            id, order_id AS orderId, product_id AS productId, product_name AS productName,
            product_image AS productImage, quantity, price, subtotal AS subtotal, created_at AS createdAt
        FROM t_order_item
        WHERE order_id = #{orderId}
    </select>

</mapper>